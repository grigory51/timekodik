[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "timekodik"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "click>=8.2,<9",
  "pydantic>=2.11,<3",
  "tqdm>=4.67,<5",
  "transcribe-cpp @ https://github.com/handy-computer/transcribe.cpp/releases/download/v0.2.1/transcribe_cpp-0.2.1-py3-none-any.whl",
]

[project.scripts]
manifestator = "manifestator.cli:cli"

[tool.setuptools]
packages = ["manifestator"]

[dependency-groups]
dev = ["mypy>=1.17,<2", "types-tqdm>=4.67,<5"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["manifestator"]
